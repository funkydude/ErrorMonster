local L = AceLibrary("AceLocale-2.0"):new("ErrorMonster")

L:RegisterTranslations("enUS", function()
	return {
		["Shows the current filters and their ID number."] = true,
		["Adds [message] to the filter list."] = true,
		["<filter>"] = true,
		["Removes the message [id] from the filter list."] = true,	
	}
end)